{
  "_comment": "Template only — copy the Razorpay values below into `dotnet user-secrets` locally, or into your host's environment variables in production. Never fill in real values here; this file is committed to source control.",
  "Razorpay": {
    "KeyId": "rzp_test_REPLACE_WITH_YOUR_RAZORPAY_TEST_KEY_ID",
    "KeySecret": "REPLACE_WITH_YOUR_RAZORPAY_TEST_KEY_SECRET"
  }
}
